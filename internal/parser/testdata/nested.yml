tasks:
  ci:
    test:
      run:
        - cmd: echo "Running tests"

    lint:
      run:
        - cmd: echo "Linting code"

    full:
      deps:
        - ci:test
        - ci:lint
      run:
        - cmd: echo "CI complete"

  dev:
    start:
      run:
        - cmd: echo "Starting dev server"
