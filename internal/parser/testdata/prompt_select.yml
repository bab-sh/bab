tasks:
  test:
    run:
      - prompt: environment
        type: select
        message: Select environment
        options:
          - dev
          - staging
          - prod
        default: dev
